# .env (example for OSM/Overpass proxy)
OVERPASS_ENDPOINT=https://overpass-api.de/api/interpreter
NOMINATIM_REVERSE=https://nominatim.openstreetmap.org/reverse
OSM_CACHE_TTL_MS=120000
OSM_MAX_RADIUS=5000
OSM_MIN_RADIUS=200
# Put a contact that OSM admins can reach if needed (per good-citizen practice)
APP_USER_AGENT=FoodFindGo/1.0 (contact: you@example.com)
